# Help Desk Microservices Project (MEAN Stack)

Welcome to your Microservices Journey!

## 📂 Project Structure

-   **`/api-gateway`** (Node/Express): Entry point for all requests.
-   **`/auth-service`** (Node/Express + MongoDB): Handles Login/Register/JWT.
-   **`/ticket-service`** (Node/Express + MongoDB): Core logic for tickets.
-   **`/notification-service`** (Node/Express): Handles emails and real-time alerts.
-   **`/client`** (Angular): The frontend UI.

## 🚀 Setup

Run `node setup.js` to initialize all services.
